{
  "name": "sitefactory-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev --local --inspect",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply sitefactory"
  },
  "dependencies": {
    "hono": "^4.2.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.1"
  }
}
